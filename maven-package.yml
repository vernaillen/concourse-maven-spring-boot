platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: latest

inputs:
  - name: git-repo

run:
  path: "git-repo/mvnw"
  args: ["-f", "git-repo/pom.xml", "package"]

params:
  MAVEN_BASEDIR=git-repo
