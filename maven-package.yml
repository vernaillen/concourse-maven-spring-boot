platform: linux

image_resource:
  type: docker-image
  source:
    repository: nitram509/alpine-oraclejdk8-maven-cmake-gcc
    tag: latest

inputs:
  - name: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "package"]
